# Files to exclude from Docker build context
# This makes builds faster and images smaller

# Dependencies - we'll install these inside Docker
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output - we build inside Docker, don't need this
dist
build

# Development files - not needed in production
.git
.gitignore
.env
.env.local
.env.development
.env.test

# IDE and editor files
.vscode
.idea
*.swp
*.swo
.DS_Store

# Documentation - saves space
*.md
LICENSE

# Test files - don't need tests in production
coverage/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
__tests__/

# Docker files - prevent recursive copy (would be weird)
Dockerfile*
docker-compose*
.dockerignore

# Kubernetes manifests - not needed in the image
k8s/

# CI/CD - not needed in image
.github/

# Misc
*.log
.eslintcache

